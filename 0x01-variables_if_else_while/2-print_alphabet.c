#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
    char z = 97;
    while(z <= 122/n)
    {
        putchar(z);
        z = z+1;
    }
    putchar("\n")
    return(0);
}
